import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
    headtext: {
        fontSize: 25,
        color: '#36464F',
        fontWeight: 'bold',
        margin: 10
    }
})

export default styles