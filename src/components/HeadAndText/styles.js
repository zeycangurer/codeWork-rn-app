import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
    headText: {
        color: '#EF5552',
        marginLeft: 10,
        fontSize: 16,
        fontWeight: 'bold'
    }
})

export default styles